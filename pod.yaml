apiVersion: v1
kind: Pod
metadata:
  name: static-webtw
  labels:
    role: myrole
spec:
  containers:
    - name: web
      image: shan0809/devopsbootcamptw
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
